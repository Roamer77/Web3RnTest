import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  contentWrapper: {
    borderWidth: 1,
    borderRadius: 5,
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 10,
    height: 40,
  },

  inputField: {
    paddingLeft: 10,
    fontSize: 14,
    fontWeight: '400',
  },
});
